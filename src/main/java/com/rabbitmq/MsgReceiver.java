/*
package com.rabbitmq;

import com.alibaba.fastjson.JSON;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;
import org.springframework.amqp.rabbit.annotation.RabbitHandler;
import org.springframework.amqp.rabbit.annotation.RabbitListener;
import org.springframework.stereotype.Component;

@Component
@RabbitListener(queues = RabbitConfig.MJX_ADDLOGISTICS_QUEUE)
public class MsgReceiver {

    private final Logger log = LoggerFactory.getLogger(this.getClass());
 
    @RabbitHandler
    public void process(String content) {

     */
/*   LogisticsOrderData logisticsOrderData = JSON.parseObject(content, LogisticsOrderData.class);

        log.info("接收处理队列A当中的消息： " + JSON.toJSONString(logisticsOrderData));*//*

    }
 
}*/
